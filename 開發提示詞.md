我是黃政文，用 Claude Code 自建 AI 工具與 SKILL。這次要開發一款「AI 驅動的自由劇情視覺小說 RPG」網頁遊戲，專案資料夾：`C:\Users\Clare\Downloads\AI Project\CharacterGame`（目前只有這份提示詞）。

## 已有的基礎（先讀，判斷哪些能沿用）

### 生圖與動畫：兩個 SKILL

兩個 SKILL 已能用 p5.js + 本機 ComfyUI 做出分鏡動畫網頁，流程經過實測：

- `C:\Users\Clare\.claude\skills\p5-comfyui-animation\`（彈性版）：SKILL.md 的「已知坑」表記錄角色外觀一致、去背、白色部位被挖空等生圖問題的對策
- `C:\Users\Clare\.claude\skills\p5-comfyui-animation-v2\`（逐步執行版）：
  - `scripts/`：gen_assets.py（呼叫 ComfyUI）、process_assets.py（rembg 去背、隔離底色殘色清除）、check_cutouts.py（去背檢查）、preview_frames.py（Playwright 截圖、p5 同名函式檢查）
  - `templates/`：engine.js、sound.js（Web Audio 合成配樂與音效）
  - `reference/`：pitfalls.md（症狀對照）、prompt-templates.md（提示詞範本）

演進脈絡：v1 驗證了「ComfyUI 生圖 → rembg 去背 → p5 組裝 → 無頭瀏覽器驗證」這條管線，v2 把它固化成填空式步驟。兩者都是「事先批次生圖、播放固定時間軸」。這次的遊戲是「遊玩中即時生圖、劇情隨玩家選擇分岔、需要持久化」，架構本質不同。預期沿用生圖與去背管線、提示詞經驗、驗證腳本，播放引擎與時間軸模型大概率要重寫。讀完後列出「沿用／改寫／新寫」三欄對照。

ComfyUI 本機環境查 GBrain `rules-comfyui-setup`：API `http://127.0.0.1:8000`、預設 Z-image_turbo 工作流、RTX 4050 Laptop 6GB VRAM、cfg 1.0（負面提示詞不生效）、每張約 40 秒、controlnet 與 clip_vision 模型資料夾是空的。

### 劇情驅動：輝達 NVIDIA API

對話與劇情生成用輝達 API（`https://integrate.api.nvidia.com/v1`，OpenAI 相容，金鑰前綴 `nvapi-`）。這個端點我踩過很多坑，全部有實測紀錄，動手前必讀：

- GBrain `nvidia-api-default-settings`：新工具每個設定欄位要填的值（串流、逾時、max_tokens、RPM、三組重試退避、必備防守 10 條、不要做的事）
- GBrain `nvidia-nim-api-gotchas`：約 300 秒閘道硬上限、改串流的原因、壞掉的粒度是模型不是供應者
- GBrain `holoteam-empty-response-retry`：HTTP 200 但內容空白的處置
- GBrain `nvidia-integrate-api-gotchas`：reasoning 模型回應形狀、`DEGRADED` 服務降級

實作細節最完整的是 HoloTeam 專案 `C:\Users\Clare\Downloads\AI Project\HoloTeam`：

- `electron/llm-proxy-ipc.ts`：輝達端點不回 CORS 標頭，瀏覽器直連會被擋，HoloTeam 改由主程序代理請求（含串流）
- `src/llm/providers/OpenAIProvider.ts`：串流解析、HTTP 200 內夾帶的錯誤物件
- `src/llm/providerRetry.ts`、`src/llm/streamIdle.ts`：重試分類與退避、串流靜默守護（註解有實測：NIM 不逐字串流 tool_call 參數，會整包緩衝到生成完才送出）
- `PLAN-provider-resilience-dual-model.md` 與 `docs/spike-reports/provider-image-rejected-s0-2026-09-25.md`：純文字模型收到圖片固定回 500、重試白等 94 秒的事故；文字模型搭配視覺模型的轉述做法；HoloTeam 內建的輝達模型範本 7 個實測只剩 1 個能用

HoloTeam 的重試實作有已知缺陷（PLAN 第 0 節第 5 點：重試吃迭代次數、退避中沒有倒數畫面、收到標頭前按停止會卡住），參考它的做法與數據，不要原樣照抄。

對這款遊戲的直接影響（讀完上述資料後驗證或修正）：

- 瀏覽器不能直連輝達端點，也不能把金鑰放在前端，一定要有本機後端代理。金鑰已放在本專案 `.env` 的 `NVIDIA_API_KEY`，只從後端讀取，不進原始碼、log、錯誤訊息
- 文字模型回應時間差距很大：550B 短請求實測 0.6 到 0.8 秒回完，兩萬 token 輸入的長生成首字節 22.1 秒。遊戲回合落在哪沒量過，每輪等待感是遊戲體驗核心問題
- 結構化輸出不走 tool_call（會失去串流），改從 content 取「最後一個可解析的 JSON 區塊」，`reasoning_content` 丟棄
- 任何會把圖片送進 LLM 的流程（例：用 AI 檢查立繪），只能送視覺模型，送進純文字模型必定 500
- 端點隨時可能降級、空回、404，遊戲不能因此卡死或遺失進度

## 遊戲要做到的事

1. **開局自定義**
   - 時空背景：玩家自由輸入，或從預設選（年代、地點、奇幻或科幻設定、故事基調）
   - AI 互動角色：名字、外觀、個性、說話方式、和主角的關係，可建立多位
   - 設定完成後，生成每位角色的立繪（至少 5 種表情：平靜、笑、生氣、難過、驚訝）與第一個場景背景

2. **AI 驅動劇情**
   - 玩家在對話框自由輸入，或點選 AI 每輪產生的 3~4 個對話選項
   - LLM 每輪回傳結構化資料（台詞、說話角色、表情、是否換場景、新場景描述、下一輪選項、世界狀態變化），前端依此切換立繪表情、背景、配樂氣氛
   - 台詞以串流即時顯示（打字機效果），不等整輪生成完
   - 新場景背景由 AI 撰寫提示詞送 ComfyUI 生成；生成過的場景與立繪重複使用，不重畫

3. **視覺小說呈現（p5.js）**：背景層、立繪層（淡入淡出、說話者高亮）、對話框、選項按鈕、場景轉場、Web Audio 配樂與音效（可沿用 sound.js 與 SKILL 的天氣粒子）

4. **存檔與節點樹**
   - 劇情切分成關卡節點（初步想法：每個場景是一個關卡節點，每次玩家回答是子節點；你評估後提出建議粒度）
   - 每個節點保存：對話紀錄、場景與立繪圖片、角色與世界狀態、LLM 接續劇情所需的前情摘要
   - 多存檔槽存檔、讀檔
   - 節點樹地圖：可從任一過去節點重新選擇回答，產生新分支，舊分支保留不覆蓋

5. **ComfyUI 設計的遊戲封面與介面模板**
   - 遊戲封面（標題畫面主視覺）
   - 介面模板：標題選單、開局設定頁、遊戲主畫面、對話框、選項按鈕、存讀檔頁、節點樹頁、設定頁、確認對話框
   - ComfyUI 只產生無文字素材（框線、底紋、按鈕底圖、裝飾、圖示），所有文字由程式渲染；框類素材用九宮格切片（9-slice）適應不同尺寸
   - 全部介面統一一套美術風格，風格定案後寫成可重用的風格字串與元件規格

## 開工前先跟我確認的決定（選擇題，每題附建議與理由）

1. 後端形式：本機 Python（可直接沿用 SKILL 的 Python 生圖與去背腳本）、Node，或 Electron（HoloTeam 模式）
2. 執行型態與目標玩家：只在我這台電腦玩，或要能給別人下載遊玩（對方沒有 ComfyUI、沒有輝達金鑰時怎麼處理）
3. 美術風格方向（日系賽璐璐、水彩、厚塗、像素等，各附一句畫面感）
4. 文字模型：等 spike 量完各候選模型的首字節、總時長、JSON 合格率後，拿數據給我選

## 品質與驗收標準

- **立繪一致性**：同一角色 5 種表情並排，髮色、瞳色、服裝、臉型看得出是同一人。這是生圖端最大風險（Z-image_turbo 人物解剖弱，沒有 IP-Adapter 或 ControlNet 模型可用）。先做 spike 比較可行做法（固定 seed 加逐字相同的外觀描述、先生一張基準圖再衍生、或其他方案），拿實際生成圖比較後才定案
- **等待不卡劇情**：LLM 每輪等待時間待 spike 實測、生圖每張約 40 秒，玩家不能乾等。要有策略（串流顯示、預先生成、背景佇列、先沿用舊場景或過場畫面、快取），並寫出實測等待時間
- **輝達端點容錯**：照 GBrain `nvidia-api-default-settings` 的參數、重試分組、必備防守實作；空回、降級、JSON 解析失敗時要重試或換候選模型，重試中畫面顯示狀態與倒數，玩家可取消，當前進度不遺失
- **去背立繪**：沿用 SKILL 的隔離底色規則，白色部位（白襯衫、白髮）不能被挖空
- **端到端驗收用真實流程**（真實輝達端點、真實 ComfyUI）：自定義一個時空背景與 2 位角色 → 玩 20 輪以上、經過 3 個以上場景 → 存檔 → 關閉重開後讀檔 → 回到第 2 個場景的節點改選另一個回答 → 確認新舊分支都在節點樹上且都能載入。全程截圖存證
- **不要**：用 mock LLM 或假圖充當完成；圖片內含文字；介面素材風格不一；金鑰出現在前端、log、截圖

## 流程

1. 讀兩個 SKILL（SKILL.md、scripts、templates、reference）、上述 GBrain 頁面與 HoloTeam 檔案，確認 ComfyUI 在線
2. 用選擇題問上面的決定（最多 3 輪）
3. 做 spike，每個附成功標準與實測數據：
   - 立繪一致性（實際生成圖並排比較）
   - 生圖耗時（立繪、背景各量 3 張）
   - 輝達文字模型：用一段模擬遊戲回合的真實請求，量候選模型的首字節、總時長、JSON 合格率、空回與錯誤次數
4. 寫完整計畫：架構圖、資料結構（節點樹、存檔格式、LLM 回應 schema）、檔案結構、里程碑（每個含驗收方式）、pre-mortem。給我看，我確認後才寫程式
5. 依里程碑實作，每個里程碑完成後附截圖與驗證報告，等我確認再進下一個

## 自我檢查

計畫交付時一併附上：

- 所有假設清單，標出最不確定的 3 項
- 以第一次玩的玩家角度，最可能卡住或失望的 3 個地方
- 沿用 SKILL 與 HoloTeam 的部分，哪裡可能不適用於這款遊戲
- 延後或做成空殼的功能寫進 `GAPS.md`

我會依自我檢查結果追問，並在每個里程碑實際試玩後回饋調整。
